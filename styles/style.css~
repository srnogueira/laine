/* Default font and size */
html {
    font-size:11pt;
    font-family: 'Quicksand', sans-serif;
    padding-left:10px;
}
/* Site header */
.laineHead {   
    display:grid;
    grid-template-columns:auto auto 1fr;
    grid-gap:10px;
    padding-bottom:10px;
    max-width:100%;
}
header h1{
    margin: 0;
    font-size: 30pt;
    font-weight: normal;
}
header input{
    font-family: 'Quicksand', sans-serif;
    font-size: 12pt;
}
/* Menus */
.laineMenu{
    border: 1px solid  #C93833;
    box-shadow: -5px 0px 0px  #C93833;
    display: grid;
    grid-template-columns: auto auto auto auto auto auto 1fr;
    align-items:center;
    max-width:100%;
}
.subMenus input, .subMenus select{
    font-family: 'Quicksand', sans-serif;    /* Required */
}
.subMenus button{
    display: flex;
}
ul{
    margin:0;    /* For Lee-Kesler */
}
button{
    font-family: 'Quicksand', sans-serif;
    color:  #C93833;
    background-color:white;
    font-size:12pt;
    padding: 10px;
    border: 0px;
    transition-duration: 0.5s;
    min-width: 60px;
    width:100%;
}
button:hover{
    color: white;
    background-color:  #C93833;
}
button:focus{
    color: white;
    background-color:  #C93833;
}
/* Hidden menus */
.plotMenuBox, .propPlotBox, .plotBox, .lkBox, .nasaBox, .HApropsBox,.propsBox,.functionBox,.fileBox{
    display:none;
    position:absolute;
    z-index:10;
    border: 1px solid #F04747;
    background-color: white;
}
/* Change z-index */
.plotMenuBox,.functionBox,.fileBox{
    z-index:11;
}   
.propPlotBox, .plotBox, .lkBox, .nasaBox, .HApropsBox,.propsBox{
    margin-top:10px;
}
.plotDropdown, .fileDropdown, .functionDropdown{
    position:relative;
    display:inline-block;
}
.errorGrid{
    display:grid;
    grid-template-columns:60px 200px;
}
.errorGrid p{
    margin:0;
}
/* For submenus */
h2{
    font-size:14pt;
    color:  #C93833;
    margin:0;
}
table{
    border-collapse: collapse;
}
table th, table td, table tr{
    border: 0px solid white;
}
.subMenus table td{
    padding: 10px 10px 0px 10px;
}
.subMenus table{
    margin-bottom: 10px;
}
/* Code and results interface */
.interface{
    display:grid;
    grid-template-columns: auto auto 1fr;
}
.problemBox{
    max-width:100%;
}
/* For render */
.mathDiv{
    font-family: 'Shippori Mincho', serif;
    display:none;
    margin-right: 20px;
    padding-top: 10px;
    padding-left: 15px;
    text-align: justify;
}
.mathDiv h1{
    font-size:16pt;
    font-weight:bold;
}
.mathDiv h2{
    font-size:14pt;
    font-weight:bold;
    color:black;
}    
.mathDiv h3{
    font-size:12pt;
    font-weight:bold;
    color:black;
}
.box {
    display: none;
}
.about, .plotDrawBox, .solBox, .errorBox{
    display:none;
    border-radius: 5px;
    padding: 10px;
    margin-bottom:10px;
    margin-top: 20px;
    border: 1px solid  #C93833;
    box-shadow: 0px -5px 0px  #C93833;
}
.about{
    display:block;
    text-align: justify;
    max-width:90%;
}
em{
    color:  #C93833;
}
canvas {
    padding-left: 10px;
    padding-right: 10px;
}
/* Results */
.out {
    margin-top: 10px;
}
.out td{
    border: 1px solid lightgray;
    border-collapse: collapse;
    padding: 4px;
    text-align:center;
}
/* Footer */
footer{
    padding-left:20px;
    border-radius: 5px;
}
footer p{
    font-size: 11pt;
    text-align: right;
}
.printFooter {
    display:none;
}
a {
    color:  #C93833;
}
/* Responsive */
@media screen and (max-width: 660px) {
    .sideBar{
	display:flex;
	flex-wrap:wrap;
	justify-content:center;
    }
    .interface{
	display:flex;
	flex-wrap:wrap-reverse;
	justify-content:center;
    }
    /* Smaller menus */
    .propsBox table td, .HApropsBox table td, .nasaBox table td, .lkBox table td{
	display: block;
	padding: 0;
    }
    .propsBox table tr, .HApropsBox table tr, .nasaBox table tr, .lkBox table tr{
	display: block;
	padding-top: 10px;
	padding-left: 10px;
    }
    .propsBox table, .HApropsBox table, .nasaBox table, .lkBox table{
	margin-bottom:10px;
    }
    /* Same size */
    input{
	width:78%;
    }    
    select{
	width:80%;
    }
}
@media print {
    .sideBar{
	display:flex;
	flex-wrap:wrap;
	justify-content:center;
    }
    .solBox{
	margin-top:20px;
    }
    .laineMenu {
	display:none;
    }
    .printFooter {
	display:block;
    }
    .interface {
	display:flex;
	flex-wrap:wrap;
	justify-content:center;
    }
}
